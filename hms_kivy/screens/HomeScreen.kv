#:kivy 1.0

<HomeScreen>:
    name: 'home'

    canvas.before:
        Color:
            rgba: hsGreen
        Rectangle:
            pos: self.pos
            size: self.size

    BoxLayout:
        orientation: 'vertical'
        spacing: dp(5)
        padding: dp(10),dp(5),dp(10),dp(5)
        GridLayout:
            id: buttons_gird
            cols:2
            spacing: dp(15)
            padding: dp(10),dp(5),dp(10),dp(5)

            # canvas.before:
            #     Color:
            #         rgba: 0, 1, .5, .5
            #     Rectangle:
            #         pos: self.pos
            #         size: self.size

            Button:
                id: projects
                font_size: '32sp'
                text: "Projects"
                on_release: root.on_projects()
            Button:
                id: boxes
                font_size: '32sp'
                text: "Boxes"
                on_release: root.on_boxes()
            # Button:
            #     id: register_rfid
            #     font_size: '32sp'
            #     text: "Register RFID"
            #     opacity: 1.0 if root.register_rfid_allowed else 0
            #     disabled: not root.register_rfid_allowed
            #     on_release: root.on_register_rfid()
            # Button:
            #     id: meeting_check_in
            #     font_size: '32sp'
            #     text: "Meeting Check In"
            #     opacity: 1.0 if root.meeting_check_in_allowed else 0
            #     disabled: not root.meeting_check_in_allowed
            #     on_release: root.on_meeting_check_in()

        GridLayout:
            id: bottom_layout
            cols:2
            size_hint_y: 0.1

            Label:
                id: balance
                text: 'Snackspace balance: £' +root.balance
                font_size: '25sp'
                halign: 'left'

            Label:
                id: member_status
                text: 'Member Status: ' + root.member_status_string
                font_size: '25sp'
                halign: 'left'
